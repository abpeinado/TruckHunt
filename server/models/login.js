const { db } = require('../../database/index.js');

// TODO: finish writing checkUsername && login functions
// module.exports.checkUsername = ( user_info ) => {
//   return db.query('SELECT * FROM vendors where ')
// }
